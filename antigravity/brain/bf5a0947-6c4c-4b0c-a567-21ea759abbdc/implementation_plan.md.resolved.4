# 第三阶段：核心启动逻辑 (The Core)

## 目标 (Goal)
扫描本地游戏版本，解析 JSON 配置文件，生成启动参数，并启动 Java 进程。

## 计划变更 (Proposed Changes)

### 数据模型更新
#### [MODIFY] [Models/GameInstance.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs)
- 完善属性：`Id`, `Type` (Release/Snapshot), `MainClass`, `MinecraftArguments`, `Libraries` 列表。

### 新增服务
#### [NEW] `Services/GameService.cs`
- **核心功能**:
    - `ScanVersions(string distinctPath)`: 扫描 `.minecraft/versions` 下的所有文件夹。
    - `ParseVersionJson(string jsonPath)`: 解析 `version.json`，提取核心启动信息。
    - **难点**: 需要处理 `inheritsFrom` (继承自其他版本，如 Forge 继承自原版)。

#### [NEW] `Services/LaunchService.cs`
- **核心功能**:
    - `BuildArguments(GameInstance game, Account account, AppConfig config)`: 拼接启动命令。
    - `LaunchGameAsync(GameInstance game, Account account)`: 启动进程。
- **启动参数生成流程**:
    1.  **Libraries 处理**: 遍历 `libraries`，检查 `rules` (系统匹配)，拼接 ClassPath (`-cp`).
    2.  **Native 处理**: 解压 `natives` 库到临时目录。
    3.  **JVM 参数**: 拼接 `-Xmx`, `-Djava.library.path`, 以及 `arguments.jvm` (高版本) 或默认参数 (低版本)。
    4.  **游戏参数**: 拼接 `arguments.game` 或 `minecraftArguments`，替换变量 (`${auth_player_name}`, `${version_name}` 等)。
    5.  **启动命令**: `java [JVM Args] [MainClass] [Game Args]`.

### UI 更新
#### [MODIFY] [Views/HomePage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/HomePage.xaml)
- 添加一个 `ComboBox` 选择游戏版本。
- 添加一个巨大的 "启动游戏" 按钮。
- 添加一个文本框或控制台区域显示游戏日志。

## 验证计划 (Verification Plan)
### 扫描测试
- 将现有的 `.minecraft` 文件夹路径配置进去。
- 确认能识别出所有安装的版本 (原版/Forge/Fabric)。

### 启动测试 (关键)
- 选择一个原版 (如 1.16.5)。
- 点击启动。
- **预期**: 游戏窗口弹出，控制台显示大量日志。
- **故障排除**: 如果闪退，检查 `natives` 是否解压正确，`classpath` 是否缺失文件。

## 依赖
- `Newtonsoft.Json`: 深度用于解析复杂的 version.json 结构。
