<UserControl x:Class="GeminiLauncher.Controls.NotificationToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             Width="350" MinHeight="70" Margin="0,10,0,10">
    <UserControl.Resources>
        <Storyboard x:Key="SlideIn">
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,-100,0,0" To="0,20,0,0" Duration="0:0:0.5">
                <ThicknessAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="5"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="SlideOut">
            <ThicknessAnimation Storyboard.TargetProperty="Margin" To="0,-100,0,0" Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseIn" Exponent="5"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="Container"
            Style="{StaticResource iOS26.GlassNotification}">
        <Grid Margin="16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="IconBorder" Grid.Column="0" Width="40" Height="40" CornerRadius="14" Background="#25FFFFFF" Margin="0,0,12,0">
                <TextBlock x:Name="IconText" Text="ℹ️" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="Notification Title" FontSize="14" FontWeight="SemiBold" Foreground="White" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="MessageText" Text="This is a notification message." FontSize="12" Foreground="#B0FFFFFF" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>

            <Button Grid.Column="2"
                    Content="✕"
                    FontSize="12"
                    Foreground="#80FFFFFF"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="8"
                    VerticalAlignment="Top"
                    Margin="4,-4,-8,0"
                    Click="CloseButton_Click"/>
        </Grid>
    </Border>
</UserControl>
