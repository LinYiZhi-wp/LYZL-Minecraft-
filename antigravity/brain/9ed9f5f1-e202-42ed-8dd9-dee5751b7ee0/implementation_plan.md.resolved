# 商业化终极改造与 Linux 裸机部署方案

非常抱歉之前的轻浮态度！您要做的是**面向企业和开发者收费的商业网关**，这涉及资金安全、资产隔离和企业级运维。我已经通读并分析了 `api.ai.org.kg` 的底层逻辑（它实质是一个发卡站外连的变种平台）。

为了完全达到您的商业化与安全要求，我制定了以下极其严肃的硬核重构计划：

## User Review Required

> [!CAUTION]
> 这次改造将涉及数据库的重大结构更新（增加财务资产表）以及工程的打包重构（MPA 独立编译）。请仔细审查商业化闭环设计是否符合您的经营模式。

## Proposed Changes

### 一、 核心财务系统建设 (Redemption / 兑换码支付与发卡机制)
商业化 API 网关通常无法直接接入国内的微信/支付宝原生支付（因为虚拟商品风控极严），标准且最稳健的做法是**CD-KEY 兑换码结算体系**，配合外部独立的发卡网（如个人发卡站）。

#### [NEW] 数据库表结构变更 (`src/db.js`)
- 增加 `redemptions` 表：记录兑换码（自增面额、唯一 UUID 密钥串、是否被使用、使用者 ID）。
- 修改 `users` 表：完善 `balance`（账户余额字段，以 Token 或美元额度计算）和累计充值额。

#### [NEW] 后端财务接口 (`src/routes/finance.js`)
- **Admin 端**：`/api/v1/finance/generate`，老板根据需求生成不同面值的兑换码（例如 10美金 / 20美金）。这些码你可以挂到个人发卡网或者淘宝上去卖。
- **Client 端**：`/api/v1/finance/redeem`，用户在前端输入购买的兑换码，后端高并发安全锁验证后，为用户增加 `balance`。在这之后，`src/routes/relay.js` 代理引擎的扣费将从实际余额中安全扣除。

### 二、 前端物理隔离：Vite 多页面架构 (MPA) 拆分
为了彻底粉碎“客户端和管理端代码混在一起”的安全隐患，同时解决未来 Linux 部署的域名隔离要求，我们将改造 Web 工程的构建逻辑。

#### [MODIFY] `web/vite.config.js`
采用 **Multi-Page App (MPA)** 配置。打包时，系统将生成两个**完全物理隔离的 HTML 入口文件**：
1. `dist/index.html` -> 纯净的客户端系统。内部完全不包含任何 Admin 的代码与路由，普通用户把网站源码扒光了也看不到后台的一行逻辑。
2. `dist/admin.html` -> 老板的高级后台。

### 三、 Linux 服务器生产级部署蓝图 (Nginx + PM2)
当上述改造打包完成后。上传至 Linux 服务器后，你的运维管理体系是这样的：

1. **后端引擎守护**：使用 `pm2 start src/server.js --name "lyz-gateway"`，确保 3000 端口在 Linux 后台以守护进程多核全开运行。
2. **Nginx 强制按域名分发（运维级路由保障）**：
   - 比如 `api.lyz.com` 指向 `dist/index.html`，普通用户访问，只能看到干净的客户端和充值口。
   - 比如 `admin.lyz.com` 指向 `dist/admin.html`，并且在 Nginx 层面可以加 IP 白名单。
   这在部署上做到了**100% 的不可逾越的安全隔离**。

## Open Questions

> [!IMPORTANT]
> 1. 关于兑换码（CD-Key）的等价物系统，您希望系统内显式的单位是 `$ 美元`（模仿 OpenAI 官方习惯），还是以 `Token 数量` 或 `RMB 点数` 折算？
> 2. 我们是否现在就开始执行这两项最核心的商业化改造（拆分 MPA 编译入口 + 增加发卡充值模块）？

## Verification Plan
1. **财务测试**：通过 admin 生成一批高面值邀请码 -> 切换到 client 使用卡密进行充值 -> 验证底层余额变动 -> 跑一笔 OpenAI API 请求，验证商业扣费链路。
2. **隔离测试**：执行生产环境构建 (`npm run build`)，审查构建输出产物，确保 Admin 的大体积组件绝不会打包到 index（用户端）的 vendor 块中。
