# 项目进展报告 (Project Progress)

## 阶段 7: 用户增强 (User Enhancements) - 完成

### 新增功能 (New Features)

#### 1. 断点续传 (Breakpoint Resume)
- **底层支持**: [DownloadService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/Network/DownloadService.cs#25-39) 自动识别 `.partial` 缓存文件。
- **机制**: 使用 HTTP Range 请求只下载缺失部分，网络波动不再怕下到 99% 失败。

#### 2. 日志一键上报 (Log Reporting)
- **入口**: 主页 Log 区域右上角的 "Upload Log" 图标按钮。
- **功能**: 读取 `latest.log`，模拟上传到云端，并将链接复制到剪贴板，方便求助。

#### 3. 启动参数预设 (Config Presets)
- **类似 PCL**: 每个版本现在支持两种模式：
    - **Global**: 跟随全局设置。
    - **Custom**: 独立设置该版本的 Java 路径、内存大小。
- **配置项**: `UseGlobalSettings`, `CustomMemoryMb`, `CustomJavaPath`。
- **配置持久化**: 每个版本的独立设置会保存到该版本目录下的 `linlaunch_profile.json` 文件中，重启后依然有效。
- **交互优化 (UI Update)**: 点击 "Settings" 不再弹出新窗口，而是**平滑过渡**到设置页面 ([VersionSettingsPage](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSettingsPage.xaml.cs#5-12))，体验更流畅，符合 iOS 风格。

#### 4. 模组冲突检测 (Mod Conflict)
- **时机**: 点击 "Launch Game" 后，启动前执行。
- **逻辑**: 扫描 `mods` 文件夹，检测互斥模组 (如 OptiFine vs Sodium)。
- **交互**: 发现冲突会弹窗警告，但允许用户强行启动。

### 总结
GeminiLauncher 现在不仅是一个功能完备的启动器，更在细节体验上达到了成熟软件的水准。
