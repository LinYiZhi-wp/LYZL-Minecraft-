# 演练 - Minecraft 启动问题综合修复

我们解决了一系列导致 Minecraft 1.19+ 启动失败、Demo 模式及内存设置无效的问题。

## 变更总览

### 1. 修复 Demo 模式 [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
**问题：** 启动器未能正确评估版本 JSON 中的 `rules`（规则），导致 `is_demo_user` 特性被错误激活，强行添加了 `--demo` 参数。

**修复：** 更新了 [CheckRules](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#424-492) 方法，支持对 `features` 字段的检查。默认将 `is_demo_user` 设为 `false`，从而禁用了演示模式。

### 2. 修复内存分配 [LaunchService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs)
**问题：** 如果用户在版本设置中未指定内存（或设置为 0），启动器可能传递无效的内存参数，或未能回退到全局设置。

**修复：** 添加了内存兜底逻辑：
- 优先使用**版本独立设置**。
- 如果版本设置无效（<= 0），回退到**全局设置**。
- 如果全局设置也无效，默认使用 **2048MB**。
- 确保 `-Xmx` 参数始终有效。

### 3. Core 启动修复（之前已完成）
- **原生库（Natives）**：修复了 [ParseLibrary](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#216-290) 路径解析，确保能正确解压 DLL。
- **日志配置（Log4j）**：自动下载和配置 1.19+ 所需的 XML 日志文件。
- **编译修复**：解决了代码中的 `CS0103` 编译错误。

## 验证

### 手动测试
1.  **Demo 模式验证**：
    - 启动 Minecraft 1.19.1。
    - 进入游戏主界面，确认没有“Play Demo World”或“Buy the game”的提示。
2.  **内存验证**：
    - 在版本设置中将内存设为 0（或留空）。
    - 启动游戏，按 `F3` 查看右上角内存分配，应为全局设置值或 2048MB。
    - 在版本设置中设为特定值（如 4096），启动游戏，确认 `F3` 显示分配了约 4096MB。
3.  **1.18.1 验证**：
    - 尝试启动 1.18.1，确认上述修复是否同样适用于该版本（理论上通用）。

### 预期结果
- 游戏正常启动，无 Demo 提示。
- 内存分配符合预期。
- `debug_launch_cmd.txt` 中不包含 `--demo` 参数。
