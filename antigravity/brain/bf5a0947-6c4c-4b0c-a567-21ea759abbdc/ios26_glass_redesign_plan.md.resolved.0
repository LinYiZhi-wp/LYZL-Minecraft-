# iOS 26 磨砂玻璃 UI 大重构

从"暗色模式"进化为"磨砂玻璃"通透质感，同时补全交互功能。

> [!IMPORTANT]
> 工作量很大，分 4 批实现。每批完成后验证 build + 视觉效果再继续。

---

## 批次 A：视觉基础层（最高优先级）

### 1. 全局材质升级
#### [MODIFY] [iOS26.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Styles/iOS26.xaml)
- 层级色从实心 `#991A1A1A` 改为半透明玻璃层：
  - `iOS26.Background` → `#40000000`（40% 黑）
  - `iOS26.ContentLayer` → `#50181818`（30% 侧边栏）
  - `iOS26.FloatingLayer` → `#80222222`（弹出层）
- 新增 `iOS26.GlassScrollBar` 样式：4px 宽，圆角 2px，半透明（`#30FFFFFF`），鼠标移入变粗到 6px + 更亮
- `HeroButtonStyle` 升级：Background 改为 LinearGradientBrush（`#00E676` → `#00C853`），增加外发光 DropShadow

### 2. 自定义滚动条（全局）
#### [NEW] [GlassScrollBar.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Styles/GlassScrollBar.xaml)
- 覆盖 `ScrollBar` 默认样式：4px 窄条、半透明、hover 展开到 8px
- 在 [App.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/App.xaml) 中全局引入，替换所有原生 Windows 滚动条

### 3. 主窗口悬浮感
#### [MODIFY] [MainWindow.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml)
- 内容区 Frame 加 `Margin="0,10,16,16"` + `CornerRadius="16"`，实现悬浮感
- 侧边栏 Border 的 `Background` 从 ContentLayer 改为 `#30000000`（更通透）

---

## 批次 B：主页重构 + 启动按钮

### 4. 主页从"Welcome"到"版本中心"
#### [MODIFY] [HomePage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/HomePage.xaml)
- 中央大字 "Welcome to LinLauncher" → 显示当前选中版本号（如 "1.20.1"），42px bold
- 副标题显示版本类型/加载器（如 "Fabric 0.15.3 · Modded"）
- 背景：保留蓝色渐变，但增加版本相关的微妙变化
- 底部新增**拼图图标按钮**（`🧩`）在版本选择和版本设置之间，用于快速打开 Mod 管理

#### [MODIFY] [HomePage.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/HomePage.xaml.cs)
- 选版后更新中央大字为版本号
- 拼图按钮点击 → 弹出简化版 Mod 列表悬浮窗

### 5. 启动按钮宝石质感
#### [MODIFY] [iOS26.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Styles/iOS26.xaml)
- `HeroButtonStyle` 的 Background → `LinearGradientBrush`（左上亮绿 → 右下深绿）
- Effect → `DropShadowEffect` Color="#00E676"，BlurRadius 50，强发光

---

## 批次 C：功能交互升级

### 6. 下载页 Action Sheet
#### [MODIFY] [DownloadPage.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/DownloadPage.xaml.cs)
- 点击"下载"不直接开始，弹出 ActionSheet 选择：
  - ☁️ 仅原版 (Vanilla)
  - 🧵 原版 + Fabric（推荐）
  - ⚒️ 原版 + NeoForge
- 选择后再开始下载+安装加载器

### 7. 内存智能警告
#### [MODIFY] [SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml)
- 在 Slider 下方增加 `x:Name="MemoryWarningText"` 警告文本（默认隐藏）

#### [MODIFY] [SettingsPage.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml.cs)
- [MemorySlider_ValueChanged](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml.cs#178-189) 中检测系统物理内存（`GC.GetGCMemoryInfo().TotalAvailableMemoryBytes`）
- 若 > 80% 物理内存 → 红色警告 "⚠️ 分配过多可能导致电脑卡死"
- 若 < 1024MB → 红色警告 "⚠️ 分配过少可能导致游戏崩溃"
- 正常范围 → 绿色 "✓ 推荐范围"

### 8. 版本选择弹窗瘦身
#### [MODIFY] [VersionSelectorDialog.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSelectorDialog.xaml)
- 左侧文件夹面板默认折叠，用一个按钮切换显示/隐藏
- 默认全宽显示版本列表

---

## 批次 D：细节打磨

### 9. 资源页卡片图片优化
#### [MODIFY] [ResourcesPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/ResourcesPage.xaml)
- 卡片图片 `Stretch="Uniform"` → `Stretch="UniformToFill"` + `RenderTransformOrigin` clip

### 10. 账号隐私模式
#### [MODIFY] [SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml)
- ACCOUNT MANAGEMENT section 增加"🔒 不保存登录信息"ToggleSwitch
- Token 状态显示（如 "Token 有效期还剩 12 天"）

---

## Verification Plan

### 每批完成后
1. `dotnet build` 确认 0 errors
2. `dotnet run` 视觉检查：
   - 批次A：窗口是否通透、滚动条是否变成细条
   - 批次B：主页是否显示版本号，按钮是否发光
   - 批次C：下载弹窗、内存警告是否工作
   - 批次D：图片裁剪、隐私模式

---

## 推荐顺序

**先做 A → B → C → D**。A 和 B 视觉冲击力最大，C 是功能完善，D 是锦上添花。
