# 演练 - 修复 Minecraft 1.19+ 启动问题

Minecraft 1.19.1+ 启动失败的原因是启动器没有处理新版本所需的 `logging`（日志）配置。

## 变更

### 1. 模型更新 [GameInstance.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs)
添加了 [LogConfig](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs#54-60)、[LogFile](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs#61-68) 和 `LogType` 类，用于表示 `version.json` 中的日志配置结构。

### 2. 服务更新 [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
更新了解析器，以读取版本 JSON 文件中的 `logging` 部分并填充 [LogConfig](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs#54-60) 属性。

### 3. 启动逻辑更新 [LaunchService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs)
- 在 [ValidateAndRepairAsync](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs#467-557) 中添加了逻辑，以验证并下载日志配置文件 (XML) 到 `.minecraft/assets/log_configs/`。
- 在启动命令中添加了 `-Dlog4j.configurationFile` JVM 参数。

## 验证

### 手动测试
1.  **启动 1.19.1**：
    - 在启动器中选择 Minecraft 1.19.1。
    - 点击“启动”。
    - 启动器应下载缺失的日志配置文件（例如 `client-1.12.xml`）。
    - 游戏应成功启动。
2.  **验证日志**：
    - 检查游戏目录中的 `debug_launch_cmd.txt`。
    - 确保其中包含 `-Dlog4j.configurationFile=...` 参数。
