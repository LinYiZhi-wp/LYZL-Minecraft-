# 实现方案：页面过渡动画 + Settings配置

## 目标
1. 为整个应用添加iOS风格的流畅页面切换动画
2. 在Settings页面添加完整的游戏配置选项

## 技术方案

### 1. 页面过渡动画
**位置**：[MainWindow.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml) 的 Frame控件

**动画效果**：
- 页面进入：从右侧滑入 + 淡入（0.3秒）
- 页面退出：向左滑出 + 淡出（0.3秒）
- 使用 `NavigationService.Navigating` 事件触发

**实现步骤**：
1. 在[MainWindow.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml.cs)中监听Frame的`Navigating`事件
2. 为Page添加RenderTransform
3. 使用Storyboard实现滑动+淡入淡出组合动画

### 2. Settings页面扩展

**新增配置项**：

#### 游戏设置卡片
- 游戏文件夹路径（TextBox + Browse Button）
- 版本隔离（Toggle）

#### Java设置卡片
- Java路径（TextBox + Browse Button）
- 自动搜索Java（Button）
- 显示检测到的Java版本

#### 内存分配卡片
- 内存分配模式（RadioButton: 自动/自定义）
- 内存滑块（Slider: 512MB - 系统最大内存）
- 实时显示分配值

**UI布局**：
- 深色半透明卡片（#40252525）
- 统一配色方案
- 清晰的标签和输入框

## 修改文件

### [MODIFY] MainWindow.xaml.cs
添加Frame导航动画逻辑

### [MODIFY] SettingsPage.xaml
完全重新设计UI，添加所有配置选项

### [MODIFY] SettingsPage.xaml.cs (如需要)
添加Java自动搜索和路径选择逻辑

## 验证计划
1. 测试页面切换动画流畅度
2. 验证所有设置选项可用
3. 确认配色统一
