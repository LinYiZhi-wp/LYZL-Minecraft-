# 第二阶段：账号与验证系统 (Authentication)

## 目标 (Goal)
实现离线登录（Offline）和微软正版登录（Microsoft OAuth 2.0），并支持多账号切换和 Token 自动刷新。

## 计划变更 (Proposed Changes)

### 数据模型更新
#### [MODIFY] [Models/Account.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/Account.cs)
- 确保包含 `RefreshToken`, `AccessToken`, `MinecraftAccessToken`, `UserType` (Mojang/Microsoft/Offline).

### 新增服务
#### [NEW] `Services/AuthenticationService.cs`
- **核心功能**:
    - `LoginOffline(string username)`: 生成离线 UUID 并返回 Account 对象。
    - `LoginMicrosoftAsync()`: 启动 OAuth 2.0 流程。
    - `RefreshSessionAsync(Account account)`: 刷新 AccessToken。
- **微软登录流程 (具体步骤)**:
    1.  **OAuth Authorize**: 打开浏览器访问 Microsoft 登录页 (Scope: `XboxLive.Signin`, `offline_access`).
    2.  **Get Token**: 通过回调获取 `Authorization Code`，交换 `Access Token`.
    3.  **Xbox Live Auth**: 用 `Access Token` 换取 `XBL Token` (RPS).
    4.  **XSTS Auth**: 用 `XBL Token` 换取 `XSTS Token`.
    5.  **Minecraft Auth**: 用 `XSTS Token` 换取 `Minecraft Access Token`.
    6.  **Profile**: 获取 Minecraft 档案 (皮肤、UUID).

#### [NEW] `Services/AccountManager.cs`
- 管理账号列表 (`ObservableCollection<Account>`)。
- 处理当前选中账号 (`CurrentAccount`)。
- 当应用启动时，尝试自动登录/刷新当前账号。

### UI 更新
#### [MODIFY] [Views/SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml)
- 添加账号管理界面：列表显示、添加账号按钮、删除按钮。
- 显示当前登录用户的头像（如果有）。

## 验证计划 (Verification Plan)
### 离线登录测试
- 输入用户名 "Steam" -> 生成 UUID -> 登录成功 -> 显示在账号列表。

### 微软登录测试
- 点击 "添加微软账号" -> 弹出浏览器 -> 登录微软账号 -> 返回启动器 -> 显示正版用户名和头像。
- 关闭启动器重开 -> 自动刷新 Token -> 保持登录状态。

## 依赖
- `System.Net.Http`: 用于所有 REST API 请求。
- `System.Security.Cryptography`: 用于生成离线 UUID。
