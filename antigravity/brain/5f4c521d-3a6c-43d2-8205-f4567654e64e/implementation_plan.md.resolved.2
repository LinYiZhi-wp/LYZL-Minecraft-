# 实现计划 - 自定义通知与错误处理

用户要求使用类似 iOS 26 风格的通知来代替原生 Windows `MessageBox` 进行启动错误提示，并改进错误报告。

## 需要用户审查
> [!IMPORTANT]
> 本计划将移除标准的 `MessageBox` 调用，改为使用新的 `NotificationManager` 机制。

## 拟定变更

### UI 组件
- **[新建] `Controls/NotificationToast.xaml`**: 自定义 Toast 通知控件（包含标题、消息、图标、类型）。
- **[修改] [MainWindow.xaml](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml)**: 添加用于放置通知的容器（如覆盖层 `Grid` 或 `StackPanel`）。
- **[修改] [MainWindow.xaml.cs](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml.cs)**: 添加显示/隐藏通知的逻辑。

### 服务
- **[新建] `Services/NotificationService.cs`**: 用于发布通知事件的服务。
- **[修改] [MainViewModel.cs](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/ViewModels/MainViewModel.cs)**: 订阅 `NotificationService` 并触发 UI 更新（或暴露 `ShowNotification` 命令）。
- **[修改] [LaunchService.cs](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs)**: 将 `MessageBox.Show` 替换为 `NotificationService.ShowError`。

### 清理缓存（用户请求的可选功能）
- **[修改] [VersionSettingsDialog.xaml](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSettingsDialog.xaml)**: 添加“修复 / 清理缓存”按钮，用于删除版本 json 和索引文件，强制重新下载。

## 验证计划
1. 触发启动错误（例如断开网络）。
2. 验证是否显示流畅的“iOS 风格”Toast 通知，而不是 MessageBox。
3. 验证 Toast 是否会自动消失或拥有关闭按钮。
