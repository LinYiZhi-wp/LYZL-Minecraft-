# 第四阶段：下载与安装系统 (Download & Install)

## 目标 (Goal)
实现支持多线程、断点续传和多镜像源（Mojang/BMCLAPI）的高效下载引擎，以及原版游戏的自动安装功能。

## 计划变更 (Proposed Changes)

### 新增服务
#### [NEW] `Services/Network/DownloadService.cs`
- **核心功能**:
    - `DownloadFileAsync(string url, string path, string? sha1 = null)`: 下载单个文件并校验。
    - `DownloadBatchAsync(List<DownloadRequest> requests, IProgress<double> progress)`: 批量并发下载。
- **技术细节**:
    - 使用 `HttpClient` 或 `System.Net.Http.SocketsHttpHandler` 进行连接池管理。
    - 使用 `SemaphoreSlim` 控制并发数量 (默认为 64)。
    - 实现简单的重试机制。

#### [NEW] `Services/InstallationService.cs`
- **核心功能**:
    - `InstallVersionAsync(string versionId)`: 安装指定版本。
- **安装流程**:
    1.  **获取元数据**: 下载 `version_manifest_v2.json` (版本列表)。
    2.  **下载核心 Json**: 下载选定版本的 JSON (如 `1.20.4.json`)。
    3.  **解析依赖**:
        - 解析 `libraries`，生成下载任务列表 (注意区分 Native 和普通 Jar)。
        - 解析 `assetIndex`，下载索引文件，然后解析出所有 Asset 对象，生成下载任务。
        - 核心 Jar (`client.jar`) 下载任务。
    4.  **执行下载**: 调用 `DownloadService` 批量下载所有文件。
    5.  **校验与清理**: 验证文件完整性。

### 配置文件更新
#### [MODIFY] [Services/ConfigService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/ConfigService.cs)
- 确保 `DownloadSource` 被正确使用。
- **源地址**:
    - Official: `https://piston-meta.mojang.com` / `https://resources.download.minecraft.net`
    - BMCLAPI: `https://bmclapi2.bangbang93.com`

### UI 更新
#### [MODIFY] [Views/DownloadPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/DownloadPage.xaml)
- **版本列表**: 显示从网络获取的版本列表 (Release/Snapshot)。
- **操作栏**: 刷新列表按钮、源切换下拉框。
- **进度展示**: 总进度条、当前下载速度、正在下载的文件名。

## 验证计划 (Verification Plan)
### 下载引擎测试
- 创建一个包含 100 个小文件的测试队列。
- 观察并发数和下载速度。
- 拔网线再插上，测试重试/续传能力 (基础版可能暂时跳过断点续传，先做重试)。

### 安装测试
- 在 Download 页选择 "1.12.2" (文件较小)。
- 点击安装。
- 观察 Libraries 和 Assets 是否完整对应到了 `.minecraft` 目录。
- 切换到 Home 页，刷新版本列表，尝试启动。

## 依赖
- `System.Threading.Tasks.Dataflow` (可选，或手动 `Select` + `WhenAll` 实现并发)。
