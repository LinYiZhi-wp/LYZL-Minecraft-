# 崩溃修复验证指南

## 概述
我们已经解决了应用程序崩溃（闪退）的问题。主要原因是在 [DownloadManagerPage.xaml](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Views/DownloadManagerPage.xaml) 中错误地使用了 `Color` 资源来设置 `Foreground`（需要 `Brush`）。此外，我们还实现了全局异常处理系统，以便未来能捕获并记录任何未处理的错误。

## 变更详情

### 1. 修复核心崩溃 (Critical Fix)
- **文件**: [Views/DownloadManagerPage.xaml](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Views/DownloadManagerPage.xaml)
- **问题**: `Foreground="{DynamicResource SystemAccentColor}"` 导致了 `System.InvalidCastException: Unable to cast object of type 'System.Windows.Media.Color' to type 'System.Windows.Media.Brush'`。
- **修复**: 修改为 `Foreground="{DynamicResource SystemAccentColorBrush}"`。

### 2. 全局异常处理 (Safety Net)
- **Log 文件**: [GeminiLauncher.log](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/bin/Debug/net8.0-windows/GeminiLauncher.log) (位于程序运行目录)
- **机制**:
  - [DispatcherUnhandledException](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/App.xaml.cs#22-28): 捕获 UI 线程崩溃。
  - `TaskScheduler.UnobservedTaskException`: 捕获后台任务崩溃。
  - `AppDomain.CurrentDomain.UnhandledException`: 捕获严重系统错误。
- **效果**: 程序不再直接闪退，而是会弹窗提示错误信息并记录日志，方便排查。

## 如何验证

1. **启动程序**: 运行 `GeminiLauncher`。
2. **进入下载管理页面**:
   - 点击侧边栏的 "Download" 按钮。
   - 或者开始一个下载任务并点击悬浮窗进入。
   - **预期**: 页面应正常加载，不会闪退。注意观察 Minecraft 图标（之前会导致崩溃的元素）是否显示正常。
3. **观察日志 (可选)**:
   - 检查 [bin\Debug\net8.0-windows\GeminiLauncher.log](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/bin/Debug/net8.0-windows/GeminiLauncher.log)。
   - 如果一切正常，日志中应只有 `Application Started` 等信息，而没有 `ERROR` 级别的日志。

## 下一步
如果仍然遇到任何异常，请查看生成的 [GeminiLauncher.log](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/bin/Debug/net8.0-windows/GeminiLauncher.log) 文件，它现在会包含详细的错误堆栈信息，请将其发送给我以便进一步分析。
