�(<Window x:Class="GeminiLauncher.Views.VersionSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="版本设置" Height="650" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource iOS26.ContentLayer}" CornerRadius="20">
        <Border.Effect><DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="Black"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Height="60" Background="#30FFFFFF">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="←" Width="45" Height="45" Margin="10,0,0,0"
                        FontSize="24" Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="BackButton_Click" Cursor="Hand" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock x:Name="TitleText" Text="版本设置" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock x:Name="SubtitleText" Text="1.21.8-forge-58.0.3" FontSize="12" Opacity="0.6" Foreground="White"/>
                </StackPanel>
                
                <Button Grid.Column="2" Content="×" Width="45" Height="45" Margin="0,0,10,0"
                        FontSize="28" Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="CloseButton_Click" Cursor="Hand" VerticalAlignment="Center"/>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Tab Navigation -->
                <Border Grid.Column="0" Background="#20FFFFFF" CornerRadius="0,0,0,20" Padding="15">
                    <StackPanel>
                        <Button x:Name="TabOverview" Content="📦 概览" Click="TabButton_Click" Tag="overview"
                                Style="{StaticResource TabButtonStyle}" IsEnabled="False"/>
                        <Button x:Name="TabSettings" Content="⚙️ 设置" Click="TabButton_Click" Tag="settings"
                                Style="{StaticResource TabButtonStyle}"/>
                        <Button x:Name="TabMods" Content="🧩 Mod管理" Click="TabButton_Click" Tag="mods"
                                Style="{StaticResource TabButtonStyle}"/>
                        <Button x:Name="TabExport" Content="📤 导出" Click="TabButton_Click" Tag="export"
                                Style="{StaticResource TabButtonStyle}"/>
                        <Button x:Name="TabMaintenance" Content="🛠️ 维护" Click="TabButton_Click" Tag="maintenance"
                                Style="{StaticResource TabButtonStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Right Content Area -->
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="20">
                    <Grid x:Name="ContentArea">
                        <!-- Content will be loaded dynamically -->
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
�(*cascade082Qfile:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSettingsDialog.xaml