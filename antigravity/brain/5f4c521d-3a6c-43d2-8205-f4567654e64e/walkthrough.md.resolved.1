# 版本隔离功能实现说明

我已经实现了版本隔离功能，现在每个游戏版本都拥有独立的 `mods`、`saves`（存档）、`config`（配置）和 `options.txt`（设置）文件。这可以防止不同 Mod 加载器和版本之间发生冲突。

## 变更概览

### 1. 配置与逻辑
- **ConfigService (配置服务)**: 在 `launcher.json` 中添加了 [VersionIsolation](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml.cs#233-245)（版本隔离）设置（默认为 `true`，即开启）。
- **GameService (游戏服务)**: 更新了版本扫描逻辑。
    - 如果隔离 **开启**: `GameDir`（游戏目录）= `.minecraft/versions/{版本ID}/`
    - 如果隔离 **关闭**: `GameDir`（游戏目录）= `.minecraft/` (旧版/混合模式行为)
    - **特例**: 如果 `versions/{版本ID}/mods` 文件夹已存在，无论设置如何，都会强制使用隔离模式（为了兼容旧的隔离设置）。

### 2. 启动流程
- **LaunchService (启动服务)**: 添加了 [ValidateAndRepairAsync](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs#190-233)（验证并修复）方法。
    - 在启动游戏前，它会检查是否所有依赖库和客户端核心文件都存在。
    - 如果缺少库文件且该文件包含下载链接，启动器会自动尝试补全下载。
    - 确保隔离后的游戏目录文件夹已创建。

### 3. UI 界面更新
- **设置页面 (Settings Page)**: 在“游戏环境”下添加了一个“版本隔离”开关。切换开关会立即重新加载版本配置。
- **主页 (Home Page)**:
    - 🧩 按钮上的“打开 Mods 文件夹”现在是一个右键/左键菜单，点击后会打开正确的隔离文件夹（即 `versions/{版本ID}/mods`，或者在隔离关闭时打开根目录的 `mods`）。
    - 修复了主页版本选择与全局状态不同步的问题。

## 验证步骤

### 1. 测试设置开关
1.以此进入 **设置 (Settings)** 页面。
2. 将 "Version Isolation" (版本隔离) 开关 **打开 (ON)**。
3. 返回 **主页**。
4. 点击 🧩 按钮 -> 选择 "📂 打开 Mods 文件夹"。
5. 验证打开的路径是否为 `.minecraft/versions/{版本ID}/mods`。

### 2. 测试传统模式 (混合模式)
1. 进入 **设置** 页面。
2. 将 "Version Isolation" (版本隔离) 开关 **关闭 (OFF)**。
3. 返回 **主页**。
4. 点击 🧩 按钮 -> "📂 打开 Mods 文件夹"。
5. 验证打开的路径是否为 `.minecraft/mods`（除非该版本文件夹下已经自己有了 mods 文件夹，这种情况下它会保持隔离）。

### 3. 验证启动
1. 启动任意游戏版本。
2. 检查日志（或控制台输出）是否有 "ValidateAndRepairAsync" 相关信息。
3. 验证在隔离开启时，`.minecraft/versions/{VersionID}/` 目录下是否生成了 `options.txt` 和 `saves` 文件夹。
