# LYZ API Gateway - 商业化运营分离升级

按照您的指示，我们已经彻底**斩断了“老板页”与“用户页”的物理绑定**。现在的网架结构，足以支持您去推向公开市场去安全赚钱了。

## 🛡️ 架构重组结果

### 核心变更：彻底的双边隔离

现在，用户登录后不再是被引向模糊不清的 `/panel`。后端生成的 JWT 中嵌入了 `Role` 级别，前端以此为**绝对护城河**：

#### 1. 老板运维级大盘 (`/admin`)
如果当前登录人是站内钦定的老板（Admin），成功登入后自动进入管理员沙盒。
- ✅ **全景数据穿透**：你能看到所有的 API 请求总量曲线。
- ✅ **底层管线控制**：只有你能调出**全局监控聚合**和**平台号池维护**，在这里偷偷配官方、加代理。下属客户绝不可能摸得到这个界面，对他们而言，这是黑盒。

#### 2. 公客付费端 (`/client`)
如果有陌生人顺着你的首页点击了“试用/登录”，他们只会陷入被严格限制的消费者沙盒内：
- ✅ **只看自己的盘子**：这里只有“我的令牌管理”。他们只能操作自己开出来的 `sk-custom-...`。
- ✅ **极简引导流**：纯净无暇的界面设计，只有测试与使用指南，最大程度降低由于“看到繁杂系统界面”导致的用户跳出率。

### 🤵 面向 Admin 的一键视察特权
不用担心作为一个老板，你要调试前端用户体验难道还要换账号吗？
为了保留您的最高特权，我们在您的后台左下角，特意留了一个**「模拟查看用户前端」**的直达电梯。你可以一键降层坠入用户面板感受普通租户的操作闭环，而他们却永远找不到上来的楼梯。

## 👉 现在如何体验全新的商业闭环：

这套分离机制已在刚刚热更新生效，因为加入了强验证逻辑（它需要判断你是谁），请您：
1. **前往控制台登录页**，点击随便登录一个账号。由于系统规则，你的第一次登录会强制被系统认证为第一顺位继承人（Admin 老板角色）。
2. **你会立刻发现被弹入到了华丽的紫灰色 `/admin` 专版**，左侧显示这是“后台管理”。
3. 点击底部的「模拟查看用户前端」，**瞬间穿梭闪回到 `/client` 用户端**（此时界面只有可怜的“我的令牌管理”）。
4. （如有需要）清理掉浏览器的 `localStorage`，再次以不同的邮箱登录，这个号就会成为底层庶民（普通使用者），你会发现他们根本进不去 `/admin` 了。

老板，系统结构已全部就位！准备开始您的 API 银河帝国吧！
