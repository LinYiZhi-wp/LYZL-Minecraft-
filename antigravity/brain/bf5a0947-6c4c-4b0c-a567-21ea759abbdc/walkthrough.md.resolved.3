# 项目进展报告 (Project Progress)

## 阶段 4: 下载与安装系统 (Phase 4: Download & Install) - 已完成

现在启动器已经具备了“自我造血”的能力，可以从网络下载并安装游戏。

### 主要变更 (Key Changes)

#### 1. 下载引擎 ([DownloadService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/Network/DownloadService.cs#25-39))
- **多线程**: 使用 `SemaphoreSlim` (默认并发 64) 实现批量并发下载。
- **校验**: 支持 SHA1 校验，确保文件完整性。
- **重试机制**: 内置 3 次重试逻辑，增加网络波动时的稳定性。

#### 2. 安装服务 ([InstallationService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/InstallationService.cs#18-26))
- **自动安装流程**:
  - 获取版本清单 (`version_manifest_v2.json`)。
  - 下载并解析版本 JSON。
  - 自动下载所有依赖库 (`libraries`)。
  - 自动下载所有资源文件 (`assets`)。
  - 自动下载核心 Jar (`client.jar`)。
- **镜像源支持**:
  - 内置了 **Official** (Mojang) 和 **BMCLAPI** (国内镜像源) 的切换逻辑。
  - 自动替换 URL 中的域名，极大提升国内下载速度。

#### 3. UI 更新 ([DownloadPage](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/DownloadPage.xaml.cs#12-105))
- **在线版本列表**: 点击 "Refresh List" 可以从网络获取最新的 Release/Snapshot 列表 (支持源切换)。
- **一键安装**: 选择版本后点击 "Install Selected" 即可触发全自动安装。
- **进度反馈**: 虽然目前进度条是模拟的 (indeterministic)，但底部状态栏会实时显示正在进行的操作。

### 验证方法 (Verification)
1.  **启动项目**：`dotnet run`
2.  **进入下载页**：点击左侧导航栏的 "Download"。
3.  **刷新列表**：
    - 确保左侧下拉框选择 "Official" 或 "BMCLAPI"。
    - 点击 "Refresh List"。
    - 等待几秒，列表中应出现版本 (如 1.20.4, 1.12.2 等)。
4.  **安装测试**：
    - 选择一个较老的版本 (如 1.12.2，文件较小)。
    - 点击 "Install Selected"。
    - 观察 `%AppData%\.minecraft\versions` 和 `libraries` 文件夹，确认文件正在生成。
    - 安装完成后，去 Home 页刷新，应该能看到该版本并启动。

---

## 下一阶段：生态整合 (Phase 5: Ecosystem)
目前我们有了原版游戏的下载和启动能力。下一阶段我们将整合 Mod 生态：
- CurseForge / Modrinth API 对接。
- 模组下载与搜索。
- 整合包导入/导出。
