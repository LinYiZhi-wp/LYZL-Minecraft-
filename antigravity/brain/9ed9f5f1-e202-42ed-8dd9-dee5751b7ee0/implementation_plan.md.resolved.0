# API 网关核心系统升级实现计划

你提到需要仿照 `api.ai.org.kg`（基于 sub2api / One-API 架构）来补全功能，并希望最终作为一个功能全面、可用于生产环境（部署到服务器）的完整项目。

当前我们的 `server.js` + `db.json` 架构更像是一个原型的单文件脚本。如果要真正放到服务器上并具备生产级的可用性和全面性，我们需要进行一次彻底的架构升级。

## User Review Required

> [!IMPORTANT]
> 这是整个项目向生产环境演进的架构蓝图。请审阅以下技术栈选择和功能范围，如果你同意，我们将按此计划逐步开始执行！
> 特别需要你确认：前端框架你是否偏好 **React** 或 **Vue3**？（目前计划采用 Vite + React + TailwindCSS）。

## Proposed Changes

### 1. 数据库持久化升级 (SQLite)
目前所有的账号、额度、上游配置、详尽的请求日志都存在本地的 `data/db.json` 中。在真正的服务器高并发调用时，极其容易造成文件损坏或读写冲突。
*   **计划**：引入 `better-sqlite3`（或结构化的 ORM如 Prisma），将核心数据按表规范化（Users, Tokens, Upstreams, UsageLogs, Subscriptions 等）。SQLite 单文件易于部署，且完全胜任中小型网关服务器的并发。

### 2. 后端核心重构 (Node.js + Express 模块化)
单一的 `server.js` 已经接近 1000 行，再堆砌如“模型倍率”、“充值体系”、“邮件验证”等功能将无法维护。
*   **计划**：
    *   将路由拆分为：`/api/user`, `/api/admin`, `/api/log`, `/api/token`, `/api/channel` 等。
    *   增强 `/v1/chat/completions` 等代理路由的健壮性：引入**多渠道负载均衡**、**故障自动重试 / Fallback**。
    *   引入完整的 **模型计费计价层**（比如 GPT-4 和 GPT-3.5 消耗的 Token 换算金额倍率不同）。

### 3. 前端界面系统升级 (Vite + SPA 框架)
你提供的目标网站拥有漂亮的落地页、黑白模式切换，以及后台多维度的仪表盘。这些很难纯靠单个 `index.html` 的原生 JS 来完美实现。
*   **计划**：
    *   使用 `Vite` 初始化一个现代前端项目结构（React 或 Vue3 根据你的喜好选择）。
    *   实现极具设计感的 **Landing Page**（带有渐变文字、卡片、暗黑模式切换）。
    *   实现 **管理后台 (Dashboard)**：包含所有 One-API 的经典模块（仪表盘、渠道管理、令牌管理、充值兑换、日志分析）。
    *   引入现代图标库和真实图表组件 (ECharts)。

### 4. 重点业务功能补齐
为了达到“功能全面”的商业化水准，将补齐以下缺失模块：
*   **计费引擎**：基于不同 Model 返回真实花费金额（而非仅计算 Token 数量）。
*   **兑换码 /充值系统**：管理员批量生成兑换码，普通用户在线核销充值。
*   **公告与系统配置**：完全不用改代码，管理员在前端自由配置网站站名、Logo、注册开关。
*   **用户邮件体系（可选）**：通过 SMTP 发送注册验证码与重置密码邮件（后续阶段）。

---

## Open Questions

1. **前端技术栈确认**：推荐使用 `Vite + React + TailwindCSS`，开发效率高且组件丰富（如果你更倾向目标网站使用的 `Vue 3` 也可以，请告诉我）。
2. **是否保留现有数据**：你是否需要我写一个数据迁移脚本，把现在的 `db.json` 的数据全部无缝转入新的 SQLite 系统中？还是我们直接用新的架构建库即可？
3. **部署环境**：后续你是计划用 Docker 部署，还是直接使用 PM2 在服务器启动？（这决定了我们后续的配置文件该怎么留）。

## Verification Plan

### Automated Tests
*   针对核心的扣费逻辑与代理转发（`/v1/chat/completions`）进行单元测试，确保扣费极度精准，不漏计、不多计。

### Manual Verification
*   完整部署前端跑通后，为你提供一个全链路演示：从新用户注册 -> 充值 -> 创建Key -> 发出AI请求 -> 余额实时扣减 -> 仪表盘呈现。
