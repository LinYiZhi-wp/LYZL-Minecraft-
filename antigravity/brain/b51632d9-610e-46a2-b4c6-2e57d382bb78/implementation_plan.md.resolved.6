# PCL 风格功能补全计划 (Implementation Plan)

## 目标
根据您提供的 PCL 截图，补全缺失的核心功能。主要重点是下载体验（国内镜像源）和高级启动选项。

## 拟定修改 (Proposed Changes)

### 1. 下载设置 (Download Settings)
**目标**: 解决国内下载慢的问题，允许用户自定义下载行为。

*   **数据模型 (`AppConfig.cs`)**:
    *   新增 `DownloadSource` (枚举: Official, BMCLAPI)。
    *   新增 `MaxDownloadThreads` (整数: 默认 64)。
*   **UI ([SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml))**:
    *   **下载源**: 下拉菜单选择 (官方源 / BMCLAPI 镜像源)。
    *   **线程数**: 滑块 (1 - 128)。
*   **逻辑 (`DownloadService.cs` / [GameService.cs](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs))**:
    *   在生成下载请求时，检查 `DownloadSource` 设置。
    *   如果是 **BMCLAPI**，将 `launchermeta.mojang.com`、`piston-meta.mojang.com` 等官方 URL 替换为 BMCLAPI 对应的镜像地址。

### 2. 高级启动设置 (Advanced Launch Settings)
**目标**: 提供更专业的启动控制。

*   **数据模型 (`AppConfig.cs`)**:
    *   新增 `GlobalJvmArguments` (字符串)。
    *   新增 `GlobalGameArguments` (字符串)。
    *   新增 `CustomWindowTitle` (字符串)。
*   **UI ([SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml))**:
    *   新增文本框用于输入上述参数。
*   **逻辑 ([LaunchService.cs](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs))**:
    *   在构建启动参数 ([LaunchGameAsync](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs#23-438)) 时，将全局 JVM 参数和游戏参数追加到命令中。
    *   实现 `-Dminecraft.launcher.title` 参数来修改窗口标题。

### 3. 界面定制 (UI Customization)
**目标**: 允许用户隐藏不常用的功能。

*   **数据模型**:
    *   新增 `HiddenPageKeys` (List<string>)。
*   **UI ([SettingsPage.xaml](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml))**:
    *   添加复选框列表：隐藏“下载页面”、“更多页面”等。
*   **逻辑 ([MainWindow.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/MainWindow.xaml.cs))**:
    *   根据配置动态控制 `NavigationView` 中菜单项的 [Visibility](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml.cs#295-303)。

## 验证计划 (Verification Plan)
1.  **测试下载源**: 切换到 BMCLAPI，尝试下载一个新版本，观察日志中的 URL 是否已变为镜像源。
2.  **测试启动参数**: 设置自定义 JVM 参数（如 `-Xmx` 或其它），启动游戏后检查 `debug_launch_cmd.txt` 确认参数是否生效。
3.  **测试界面隐藏**: 勾选隐藏“下载页面”，确认侧边栏该选项立即消失。
