# 演练 - Minecraft 启动问题综合修复

我们解决了一系列导致 Minecraft 1.18.2/1.19+ 启动失败、崩溃及配置无效的问题。

## 变更总览

### 1. 修复库解析导致的崩溃 (关键修复) [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
**问题：** 启动器原有的 `ParseLibrary` 方法逻辑存在缺陷。当一个库条目即包含标准 Artifact（代码 Jar）又包含 Natives（原生库）时（例如 LWJGL），代码 Jar 的路径会被原生库路径**覆盖**。这导致生成的 Classpath 中只包含原生库 Jar，而缺失了实际的代码 Jar，引发 `java.lang.NoClassDefFoundError: org/lwjgl/system/MemoryUtil` 等崩溃。

**修复：** 
- 将 `ParseLibrary` 重构为 [ParseLibraries](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#219-307)，使其能返回多个 [Library](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/GameInstance.cs#37-46) 对象。
- 现在，对于包含 Natives 的条目，启动器会**同时**解析并添加代码 Jar 和原生库 Jar。
- 配合 [AddOrReplaceLibrary](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#542-576) 方法，确保同名库的新版本覆盖旧版本（去重）。

### 2. 修复 Demo 模式 [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
**问题：** 启动器错误激活 `is_demo_user` 特性。
**修复：** 默认禁用演示模式。

### 3. 修复内存分配 [LaunchService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs)
**问题：** 内存参数可能无效。
**修复：** 实现了“自定义 -> 全局 -> 默认 2048MB”的兜底逻辑。

### 4. Core 启动修复
- **日志配置**：支持 1.19+ Log4j XML。
- **原生库路径**：修复 DLL 解压路径。

## 验证

### 手动测试
1.  **1.18.2 启动测试**：
    - 启动 Minecraft 1.18.2。
    - **观察：** 游戏应正常启动。
    - **检查日志：** 确认 Classpath 中**同时**包含 `lwjgl-3.2.2.jar`（代码）和 `lwjgl-3.2.2-natives-windows.jar`（原生，虽然原生通常不加 CP 但加了也没坏处，关键是代码 Jar 必须在）。
2.  **Demo 模式验证**：无 Demo 提示。
3.  **内存验证**：F3 显示正确内存。

### 预期结果
- 游戏正常启动，无崩溃。
