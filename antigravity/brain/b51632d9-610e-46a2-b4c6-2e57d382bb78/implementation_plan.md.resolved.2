# Implementation Plan - Fix Launch Issues

## Goal
Fix remaining issues preventing proper gameplay:
1.  **Demo Mode**: (Partially Fixed) Ensure logic is solid.
2.  **Memory Allocation**: (Fixed) Logic updated.
3.  **1.18.1/1.18.2 Crash**: Fix duplicate libraries causing native crashes.

## User Review Required
> [!NOTE]
> No major user review needed for library fix, it's a standard correctness fix.

## Proposed Changes

### [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
-   **Add `AddOrReplaceLibrary` Method**:
    -   Identify libraries by `Group:Artifact[:Classifier]`.
    -   When adding a library, remove any existing library with the same identity (ignoring version).
    -   This ensures we only have one version of any given library (e.g., Lwjgl 3.2.2 replaces 3.2.1).
-   **Update [ParseVersionJson](file:///c:/Users/Linyizhi/.gemini/GeminiLauncher/Services/VersionDetectionService.cs#111-172)**:
    -   Use `AddOrReplaceLibrary` instead of `instance.Libraries.Add`.

## Verification Plan
### Manual Verification
1.  **Launch 1.18.2**:
    -   Check logs to ensure only ONE version of LWJGL natives is extracted.
    -   Confirm game launches without `EXCEPTION_ACCESS_VIOLATION`.
