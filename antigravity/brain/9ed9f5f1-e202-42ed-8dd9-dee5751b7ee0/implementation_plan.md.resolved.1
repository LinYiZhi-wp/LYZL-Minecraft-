# 后台系统架构分离与商业化改造计划 (Role Separation)

为了将本项目提升至可商用级别，目前的“大杂烩”控制台必须进行**严格的权限隔离与路由拆分**。不能让普通用户看到全局的号池、模型配置以及系统级数据。

## User Review Required

> [!CAUTION]
> 这是一个重大的前端架构调整，将涉及到用户交互侧的完全重写。请您确认本方案的路由分发规则与隔离机制是否符合您的产品商业化设想。

## Proposed Changes

我们将从根本上将前端拆分成完全独立的两套视图与路由，依靠 JWT 鉴权与 LocalStorage 数据进行阻断。

### Frontend Auth (Local Storage & Token)

- 登录/注册成功后，除了存储 `lyz_session_token` 之外，提取 `user.role` 并持久化。
- 路由端(`src/router/index.js`)引入权限校验 (Navigation Guards)。

---

### [NEW] Admin Panel (`/admin`)

只有 `role === 'admin'` 的用户可访问。这里是**平台老板的运营指挥中心**。

#### [NEW] `src/views/AdminLayout/index.vue`
- 左侧边栏菜单设计，仅包含：
    - **全局数据罗盘**: 统计平台上所有用户的总消耗、池子成本估算。
    - **号池配置 (New API 核心)**: 等同于原先的 `Channels` 组件，让站长填官方 Key。
    - **用户管理**: 查看平台上注册的全部用户及余额分布（后续可加充值功能）。

---

### [NEW] Client Panel (`/client`)

任何注册用户（包含 Admin 测试用）均可访问。这里是**向掏钱买单的客户交付的网页**。

#### [NEW] `src/views/ClientLayout/index.vue`
- 左侧边栏菜单设计，极简聚焦，完全屏蔽底层引擎设定：
    - **我的用量**: 当前账户余额，今日个人花了多少 Token，画出单用户的曲线图。
    - **令牌密钥**: 在这里点击创建 `sk-custom-...`。
    - **接入测试 (Playground)**: 测试聊天框，并且附带各种客户端（如 ChatNextWeb，OpenCat）的填入教程提示。

---

### [MODIFY] `src/views/Login.vue`
- 在成功获得 Login 响应后，判断 `user.role`：
    - 若为 `admin` -> 直接跳转至 `/admin`
    - 若为 `viewer` (普通用户) -> 直接跳转至 `/client`

## Open Questions

> [!IMPORTANT]
> 1. 您是否需要在一个超级管理员账号上**同时**浏览两端？（即在 Admin 界面预留一个 "切换至用户端测试" 的按钮，方便老板测试通道？）
> 2. 当前系统普通用户新注册默认是不带余额的免费版本，需要做“付费弹窗”或者“充值引导框”预留位吗？

## Verification Plan
1. **角色切换测试**: 手动在数据库中将账号设置成普通用户，验证前端无论如何尝试也**绝对无法**通过拼接 URL 等方法绕过进入 `/admin/channels`。
2. **老板体验流闭环**: 使用 Admin 账号登入，能够同时管理“下层用户”与“上层接口号池”。
