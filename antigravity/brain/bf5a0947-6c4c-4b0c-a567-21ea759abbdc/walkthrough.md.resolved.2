# 项目进展报告 (Project Progress)

## 阶段 3: 核心启动逻辑 (Phase 3: Core Launch Logic) - 已完成

这是启动器的"心脏"，我们已经实现了扫描和启动游戏的核心逻辑。

### 主要变更 (Key Changes)

#### 1. 游戏版本扫描 ([GameService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#10-141))
- 自动扫描 `.minecraft/versions` 文件夹。
- 解析 `version.json`，提取核心信息：
  - `Id`: 版本号 (如 1.20.4)
  - `MainClass`: 入口类
  - `Libraries`: 依赖库列表
  - [Arguments](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#110-140): 启动参数 (JVM 和 Game)

#### 2. 启动服务 ([LaunchService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs#11-131))
- 实现了复杂的参数拼接逻辑：
  - **ClassPath**: 拼接所有 library 路径和 client.jar。
  - **Native**: 预留了解压 native 库的接口（目前使用占位符）。
  - **JVM Args**: 配置内存 (`-Xmx`) 和 Java 库路径。
  - **Game Args**: 解析并替换变量 (`${auth_player_name}`, `${version_name}` 等)。
- **进程启动**:
  - 使用 `Process` 类启动 javaw.exe。
  - 能够重定向并捕获标准输出 (StdOut) 和错误输出 (StdErr)。

#### 3. 配置文件 ([ConfigService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/ConfigService.cs#17-60))
- 添加了 `GamePath` 字段，支持自定义 `.minecraft` 路径（默认为 `%AppData%\.minecraft`）。

#### 4. UI 更新 ([HomePage](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/HomePage.xaml.cs#7-12))
- **Version Selector**: 添加了下拉框，列出所有扫描到的版本。
- **Launch Button**: 添加了主要的启动按钮。
- **Log Area**: 添加了日志显示区域（目前暂为占位符）。

### 验证方法 (Verification)
1.  **准备环境**:
    - 确保 `%AppData%\.minecraft\versions` 下至少有一个游戏版本 (例如 `1.16.5`)。
    - 确保已安装 Java，且路径正确 (代码中暂时硬编码了路径用于测试，后续会添加自动扫描)。
2.  **设置账号**:
    - 在 Settings 页添加一个离线账号。
3.  **启动游戏**:
    - 在 Home 页选择版本。
    - 点击 "Launch Game"。
    - 观察 VS Code 的 `Debug Console` 输出 (因为我们用了 `Debug.WriteLine` 输出游戏日志)。

---

## 下一阶段：下载与安装系统 (Phase 4: Download & Install)
目前我们只能启动 *已经存在* 的游戏。下一阶段我们将实现：
- 多线程下载引擎。
- 从 Mojang/BMCLAPI 镜像源下载游戏文件。
- 补全缺失的 Libraries 和 Assets。
