# 演练 - Minecraft 启动问题综合修复

我们解决了一系列导致 Minecraft 1.18.2/1.19+ 启动失败、崩溃、配置无效及 UI 缺失的问题。

## 变更总览

### 1. 修复版本设置 UI 缺失 [VersionSettingsDialog.xaml.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSettingsDialog.xaml.cs)
**问题：** 用户反馈在开启版本隔离后，版本设置页面找不到内存设置选项。代码审查发现该页面的 UI 构建逻辑中确实遗漏了内存控件。
**修复：** 在 [ShowSettingsTab](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/VersionSettingsDialog.xaml.cs#196-358) 方法中添加了“最大内存”滑块（Range: 1024MB - 16GB），支持实时显示数值并保存到版本配置中。

### 2. 修复库解析导致的崩溃 (关键修复)
**问题：** 启动器原有的 `ParseLibrary` 方法逻辑存在缺陷，导致代码 Jar 被原生库路径覆盖，引发 `NoClassDefFoundError`。
**修复：** 重构为 [ParseLibraries](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#219-307)，正确解析并返回所有 Artifact。

### 3. Core 启动修复 (已验证)
- **重复库去重**：修复 LWJGL 版本冲突。
- **Demo 模式**：禁用演示模式。
- **内存兜底**：实现 Auto -> Global -> Default 内存分配策略。
- **Log4j XML**：支持高版本日志配置。

## 验证

### 手动测试
1.  **UI 验证**：
    - 打开任意版本的设置页面（右键 -> 设置）。
    - 切换到“设置”标签页。
    - **观察：** 确认“启动选项”下出现了“最大内存”滑块。
    - 拖动滑块，确认数值变化，并点击“保存配置”。
    - 重新打开确认数值已保存。
2.  **1.18.2 启动测试**：
    - 启动 Minecraft 1.18.2。
    - 确认游戏正常启动且使用了设定的内存值（F3 查看）。

### 预期结果
- UI 显示正常，配置可保存。
- 游戏稳定运行。
