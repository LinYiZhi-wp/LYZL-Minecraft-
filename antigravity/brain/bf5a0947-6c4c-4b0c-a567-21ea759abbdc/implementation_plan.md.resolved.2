# 第一阶段：核心架构搭建 (The Foundation)

## 目标 (Goal)
搭建基于 MVVM 模式的 WPF 项目，集成必要的依赖库，并定义核心数据模型。

## 计划变更 (Proposed Changes)

### 项目结构 (手动创建)
由于环境中未检测到 .NET SDK，我们将手动创建项目文件。

- 创建解决方案 `GeminiLauncher.sln`
- 创建 WPF 项目 `GeminiLauncher`
- 创建核心源文件：
    - `GeminiLauncher.csproj`: 定义依赖项 (WPF UI, Newtonsoft.Json, SharpZipLib).
    - `App.xaml` / `App.xaml.cs`: 应用程序入口点.
    - `MainWindow.xaml` / `MainWindow.xaml.cs`: 主窗口.
    - `ViewModels/MainViewModel.cs`: 核心视图逻辑.

### 依赖库 (通过 .csproj)
`GeminiLauncher.csproj` 将引入以下 `PackageReference`:
- **WPF-UI**: 用于现代外观 (Fluent Design).
- **Newtonsoft.Json**: 用于强大的 JSON 处理 (Minecraft JSON 解析).
- **SharpZipLib**: 用于解压游戏文件/整合包.
- **CommunityToolkit.Mvvm**: (建议) 极大减少 MVVM 样板代码.

### 数据模型 (Data Models)
#### [NEW] `Models/GameInstance.cs`
- 属性：`Version` (版本), `GameDir` (游戏目录), `Mods` (模组列表), `JavaPath` (Java路径), `JvmArgs` (JVM参数).

#### [NEW] `Models/Account.cs`
- 属性：`Username` (用户名), `Uuid`, `AccessToken` (访问令牌), `AccountType` (账户类型: 离线/微软).

#### [NEW] `Services/ConfigService.cs`
- 功能：加载/保存全局设置 (下载源, 最大内存, Java安装路径).

## 验证计划 (Verification Plan)
### 自动化测试 (Automated Tests)
- `GameInstance` 解析逻辑的单元测试 (后续添加).
- `ConfigService` 读写功能的单元测试.

### 手动验证 (Manual Verification)
- 启动空白应用程序窗口.
- 验证 "WPF UI" 样式是否正确应用.
- 验证 `ConfigService` 是否在启动时创建配置文件.
