# 项目进展报告 (Project Progress)

## 阶段 5 & 6: 生态与体验 (Ecosystem & UX) - 进行中

### 新增功能 (New Features)

#### 1. 依赖自动补全 (Dependency Auto-completion)
- **原理**: 当下载一个模组时，程序会递归检查其声明的依赖关系。
- **自动化**: 如果发现某个依赖是 `required` (必须的)，程序会自动搜索并下载该依赖的对应版本。
- **去重**: 系统会智能记录已处理的项目，防止死循环或重复下载。

#### 2. 智能版本匹配 (Smart Version Matching)
- **机制**: 当您在 Home 页选择了某个游戏版本 (例如 `1.20.1`)，在资源中心下载模组时，启动器会**自动过滤**出只兼容该版本的模组文件。

#### 3. iOS 26 UI 风格
- 窗口尺寸锁定 1100x700。
- 全局 iOS 26 风格毛玻璃特效。

### 验证方法 (Verification)
1.  **设置环境**: 选择 **1.20.1** (或您有的版本)。
2.  **搜索**: 搜索 **"Sodium"** (Fabric 必须) 或 **"Iris"**。
3.  **下载**: 点击 Iris 的下载按钮。
    - **观察**: Iris 依赖 Sodium。
    - **结果**: 您应该会发现 `mods` 文件夹里**通过一次点击**，同时出现了 `Iris.jar` 和 `Sodium.jar`。

---

## 下一步计划
- **整合包导入**: 支持导入 `.mrpack`。
- **构建完善**: 修复 UI 中的小细节 (如过渡动画)。
