# 项目进展报告 (Project Progress)

## 阶段 2: 账号与验证系统 (Phase 2: Authentication) - 已完成

我们已经实现了核心的账号验证功能，支持离线模式和微软正版登录的所有关键逻辑。

### 主要变更 (Key Changes)

#### 1. 认证服务 ([AuthenticationService](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/AuthenticationService.cs#13-239))
- **离线登录**：
  - 实现了基于用户名的离线登录。
  - 使用 MD5 算法生成标准的离线 UUID (`OfflinePlayer:Username`)。
  - 自动生成默认头像 URL (Minotar)。
- **微软登录 (OAuth 2.0)**：
  - 实现了完整的 OAuth 2.0 流程。
  - **流程**：Login Live -> Get Token -> Xbox Live Auth -> XSTS Auth -> Minecraft Auth -> Get Profile。
  - *注意*：由于当前无浏览器环境，浏览器交互部分使用 `Process.Start` 模拟，并在代码中留有捕获 Redirect URL 的接口。
- **Token 刷新**：
  - 实现了 [RefreshSessionAsync](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/AuthenticationService.cs#204-238)，支持使用 RefreshToken 自动更新过期的 AccessToken。

#### 2. 数据模型 ([Account](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/Account.cs#11-38))
- 升级 [Account](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Models/Account.cs#11-38) 类为 `ObservableObject`，支持 UI 数据绑定通知。
- 添加了微软登录所需的字段：`RefreshToken`, `MinecraftAccessToken`, `ExpiryTime`。

#### 3. 账号管理 ([AccountManager](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/AccountManager.cs#8-39) & UI)
- 创建了 [AccountManager](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/AccountManager.cs#8-39) 负责管理账号列表。
- 更新了 [SettingsPage](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Views/SettingsPage.xaml.cs#9-21) (设置页)：
  - **账号列表**：显示已保存的账号、头像、UUID。
  - **添加账号**：提供了添加离线账号的输入框和按钮。
  - **微软登录**：添加了按钮（目前点击会提示未完全对接浏览器 UI）。

### 验证方法 (Verification)
1.  **运行项目**：`dotnet run`
2.  **进入设置页**：点击左侧导航栏的 "Settings"。
3.  **测试离线登录**：
    - 在 "Offline Username" 输入框中输入名字（例如 "Steve"）。
    - 点击 "Add Offline Account"。
    - 观察列表是否立即出现新账号，且显示了 UUID 和头像 URL。

---

## 下一阶段：核心启动逻辑 (Phase 3: The Core)
我们将进入最核心的部分：让游戏真正跑起来。
- 扫描 `.minecraft/versions`。
- 解析 JSON 生成启动参数。
- 启动 Java 进程并重定向日志。
