# 演练 - Minecraft 启动问题综合修复

我们解决了一系列导致 Minecraft 1.18.2/1.19+ 启动失败、崩溃及配置无效的问题。

## 变更总览

### 1. 修复重复库导致的崩溃 [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
**问题：** 1.18.2 等版本继承自父版本，导致同名库（如 LWJGL）的不同版本（3.2.1 和 3.2.2）同时被加载，引发 `EXCEPTION_ACCESS_VIOLATION` 崩溃。

**修复：** 实现了 [AddOrReplaceLibrary](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#528-562) 方法。当解析版本 JSON 时，如果有同名库（Group:Artifact:Classifier 相同），新版本（子版本）将覆盖旧版本（父版本），确保 Classpath 中只有唯一的正确版本。

### 2. 修复 Demo 模式 [GameService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs)
**问题：** 启动器未能正确评估版本 JSON 中的 `rules`（规则），导致 `is_demo_user` 特性被错误激活，强行添加了 `--demo` 参数。

**修复：** 更新了 [CheckRules](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#427-495) 方法，支持对 `features` 字段的检查。默认将 `is_demo_user` 设为 `false`，从而禁用了演示模式。

### 3. 修复内存分配 [LaunchService.cs](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/LaunchService.cs)
**问题：** 如果用户在版本设置中未指定内存（或设置为 0），启动器可能传递无效的内存参数，或未能回退到全局设置。

**修复：** 添加了内存兜底逻辑：
- 优先使用**版本独立设置**。
- 如果版本设置无效（<= 0），回退到**全局设置**。
- 如果全局设置也无效，默认使用 **2048MB**。

### 4. Core 启动修复（之前已完成）
- **日志配置（Log4j）**：自动下载和配置 1.19+ 所需的 XML 日志文件。
- **原生库路径解析**：修复了 [ParseLibrary](file:///C:/Users/Linyizhi/.gemini/GeminiLauncher/Services/GameService.cs#219-293)，确保能正确解压 DLL。

## 验证

### 手动测试
1.  **1.18.2 启动测试**：
    - 启动 Minecraft 1.18.2。
    - **观察：** 游戏不再崩溃，日志中不再出现类似 `Native library already loaded` 或 `EXCEPTION_ACCESS_VIOLATION` 的错误。
    - 检查日志，确认 Classpath 中只包含一个版本的 LWJGL（应为 3.2.2）。
2.  **Demo 模式验证**：
    - 启动任意版本，确认无 Demo 提示。
3.  **内存验证**：
    - 确认内存参数 `-Xmx` 正确传递。

### 预期结果
- 游戏正常启动，稳定运行。
- `debug_launch_cmd.txt` 中的 Classpath 干净且无重复库。
